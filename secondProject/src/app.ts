let age;

age = 30;

const username = "Max";

console.log(`${username} lele`);

const button = document.querySelector('button');
button?.addEventListener('click', () => {
    console.log('clicked');
});